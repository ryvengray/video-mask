# 视频打码服务部署指南（NVIDIA GPU）

本文适用于一台全新的 Ubuntu 22.04/24.04 NVIDIA GPU 服务器，部署本项目的优化脚本 `video_mask_batch_flast.py`。

> 当前版本状态：OWLv2 已支持动态选择 **CUDA → MPS → CPU**；`--pipe` 管道模式在现有测试素材上会产生损坏画面，生产环境请不要启用。默认文件模式可用，但会生成大量 JPEG 临时帧，需要高速磁盘和足够空间。

## 0. 确认部署版本

优化脚本已合并到 `main`。在服务器上克隆后，确认包含该文件且工作树干净：

```bash
cd /opt/video-mask
git pull --ff-only origin main
git log -1 --oneline
test -f video_mask_batch_flast.py && echo "优化脚本已就绪"
```

## 1. 推荐规格

| 场景 | 推荐规格 |
|---|---|
| 单路 1080p、测试 | 8 vCPU / 32GB RAM / 16GB NVIDIA 显存 / 500GB NVMe |
| 单路 4K 或小批量 | 16 vCPU / 64GB RAM / NVIDIA L4 24GB / 1TB NVMe |
| 多路并发 | 24+ vCPU / 128GB RAM / 1–2 张 24GB 显存卡 / 2TB NVMe |

不要以多进程同时抢占同一张 GPU 的方式提高吞吐；当前脚本是单视频、单帧推理。先保证一条稳定的 GPU worker，再进行受控的批处理调度。

## 2. 系统初始化

以下命令以普通部署账号执行；仅安装系统依赖时使用 `sudo`。

```bash
sudo apt update
sudo apt install -y git ffmpeg python3 python3-venv python3-pip

# NVIDIA 驱动应由云镜像或云厂商控制台安装；完成后必须能看到 GPU。
nvidia-smi
ffmpeg -version
```

`nvidia-smi` 无法运行时，不要继续安装 PyTorch：先按云厂商提供的驱动/CUDA 镜像或驱动文档处理。PyTorch 的 CUDA wheel 必须与驱动支持的 CUDA 版本匹配。

### 自动引导（推荐）

代码克隆完成后，可让脚本完成第 2–5 节的大部分操作：

```bash
cd /opt/video-mask
bash scripts/bootstrap_server.sh
```

脚本会检测 `nvidia-smi`；检测成功则安装 CUDA PyTorch，否则安装 CPU PyTorch。默认使用 CUDA 12.8 wheel 索引；若 PyTorch 官方安装页给你的服务器推荐其他版本，显式指定：

```bash
bash scripts/bootstrap_server.sh \
  --torch-index-url https://download.pytorch.org/whl/cu126
```

模型下载需要约 600MB 流量；受限网络或镜像已预置模型时可跳过：`bash scripts/bootstrap_server.sh --skip-models`。

## 3. 拉取代码并创建 Python 环境

```bash
sudo mkdir -p /opt/video-mask
sudo chown "$USER":"$USER" /opt/video-mask

git clone https://github.com/ryvengray/video-mask.git /opt/video-mask
cd /opt/video-mask

python3 -m venv .venv
source .venv/bin/activate
python -m pip install --upgrade pip
```

如果服务器不能使用 SSH 拉取仓库，改为 HTTPS 地址即可。

## 4. 安装 CUDA 版 PyTorch 与项目依赖

先在 [PyTorch 官方安装页](https://pytorch.org/get-started/locally/) 选择 **Linux / Pip / Python / 与当前驱动匹配的 CUDA**，并执行页面生成的命令。不要直接安装默认的 CPU wheel。

示例（仅当官方页面为你的环境给出相同 CUDA 轮子时才使用）：

```bash
python -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
python -m pip install -e '.[cards]'
```

验证 GPU：

```bash
python - <<'PY'
import torch
print("PyTorch:", torch.__version__)
print("CUDA available:", torch.cuda.is_available())
if torch.cuda.is_available():
    print("GPU:", torch.cuda.get_device_name(0))
    print("CUDA runtime:", torch.version.cuda)
PY
```

必须看到 `CUDA available: True`。PyTorch 官方也建议使用 `torch.cuda.is_available()` 确认驱动和 CUDA 能被 Python 访问。[安装与验证说明](https://docs.pytorch.org/get-started/locally/)

## 5. 预下载模型（建议在上线前完成）

优化脚本目前以离线模式加载模型。部署时应先把模型下载到 Hugging Face 缓存，避免生产任务中联网失败。

```bash
source /opt/video-mask/.venv/bin/activate

python - <<'PY'
from huggingface_hub import snapshot_download

snapshot_download("google/owlv2-base-patch16-ensemble")
print("OWLv2 缓存完成")
PY
```

YuNet 人脸模型不会被当前脚本自动下载（尽管旧文档有此描述）。从 OpenCV/Hugging Face 下载 `face_detection_yunet_2023mar.onnx` 后放入项目的 `models/` 目录：

```bash
mkdir -p /opt/video-mask/models
# 将已下载的 face_detection_yunet_2023mar.onnx 上传或复制到此目录
ls -lh /opt/video-mask/models/face_detection_yunet_2023mar.onnx
```

生产运行时始终传 `--model-dir /opt/video-mask/models`。若不存在该文件，脚本会退回 Haar；它对侧脸、小脸以及复杂背景的误检更多。

## 6. 首次冒烟测试

准备一个不包含敏感真实数据的短视频，例如 `/data/video-mask/input/test.mp4`：

```bash
cd /opt/video-mask
source .venv/bin/activate

python video_mask_batch_flast.py /data/video-mask/input/test.mp4 \
  --model-dir /opt/video-mask/models \
  --out-dir /data/video-mask/output
```

检查日志必须同时包含：

```text
[信用卡] OWLv2 模型已加载 (CUDA GPU: ...)
[人脸] YuNet 模型(...)
```

若日志出现 `降级几何法` 或 `Haar 级联`，任务虽然会继续，但并没有使用预期的高精度模型；应修复模型缓存或路径后重跑。完成后检查：

```bash
ffprobe -v error -show_entries format=duration -show_entries stream=codec_type \
  -of default=noprint_wrappers=1 /data/video-mask/output/masked_test.mp4
```

输出应有 `video`，且源视频有声音时也应有 `audio`。

## 7. 正式运行

```bash
cd /opt/video-mask
source .venv/bin/activate

# 处理一个目录。静态画面可将 card-key-int 增加到 20；手持/快速移动卡建议保持 10。
python video_mask_batch_flast.py /data/video-mask/input \
  --model-dir /opt/video-mask/models \
  --out-dir /data/video-mask/output \
  --card-conf 0.35 \
  --card-key-int 10 \
  --owl-size 1280 \
  --face-int 3
```

参数建议：

| 参数 | 建议 | 含义 |
|---|---|---|
| `--card-conf 0.35` | 起始值 | 降低低分误检；应结合业务素材调优 |
| `--card-key-int 10` | 手持/运动卡 | 每 10 帧跑一次 OWLv2；更高值更快但更易漏检 |
| `--owl-size 1280` | 默认 | 4K 视频的均衡配置；`0` 是原图推理，明显更慢 |
| `--face-int 3` | 默认 | 每 3 帧检测一次人脸，中间帧光流跟踪 |

**不要使用 `--pipe`**，直到该模式修复并通过视频完整性回归测试。

## 8. 目录、权限与批处理

推荐将数据与代码分开：

```text
/opt/video-mask/                 # Git 代码、.venv、模型文件
/data/video-mask/input/          # 待处理视频
/data/video-mask/output/         # 处理结果
```

运行账号需要对 `/data/video-mask/input` 有读权限、对 `/data/video-mask/output` 有写权限。由于当前默认文件模式会生成中间 JPEG，长 4K 任务前应确认 NVMe 空闲空间；不要将临时目录放在小容量系统盘。

可用 `tmux` 运行长任务：

```bash
tmux new -s video-mask
cd /opt/video-mask && source .venv/bin/activate
python video_mask_batch_flast.py /data/video-mask/input --model-dir models --out-dir /data/video-mask/output
# Ctrl-b d 退出 tmux，任务继续运行
```

## 9. 运维检查与故障定位

```bash
# GPU 是否正在工作
nvidia-smi -l 2

# ffmpeg 是否具备 NVIDIA 硬件编码器（可选加速）
ffmpeg -hide_banner -encoders | grep nvenc

# 当前分支和未提交改动
cd /opt/video-mask && git status --short && git log -1 --oneline
```

| 现象 | 处理方式 |
|---|---|
| 日志显示 CPU | 检查 `nvidia-smi`；重新安装与驱动匹配的 CUDA 版 PyTorch；再次执行第 4 节验证 |
| 日志显示“降级几何法” | 重新预下载 OWLv2，确认 Hugging Face 缓存对运行账号可读 |
| 日志显示 Haar | 将 YuNet ONNX 放到 `--model-dir` 指定目录 |
| 输出视频无效或条纹 | 确认没有使用 `--pipe`；使用默认文件模式 |
| 视频被音轨截短 | 当前脚本使用 `-shortest`；需修复合成参数后再用于时长严格的生产任务 |
| 磁盘迅速增长 | 清理失败任务遗留的 `vmb_*` 临时目录；预留更大 NVMe；后续修复管道模式 |

## 10. 上线前检查清单

- [ ] `nvidia-smi` 正常，且 PyTorch 输出 `CUDA available: True`
- [ ] 日志显示 `CUDA GPU`，而不是 CPU 或回退几何法
- [ ] YuNet ONNX 已放进 `models/`，日志显示 YuNet
- [ ] 短视频结果包含视频和音频流，时长符合预期
- [ ] 未使用 `--pipe`
- [ ] 输入、输出、模型缓存和临时文件有足够磁盘空间
- [ ] 用真实业务素材验证卡片、人脸的漏检与误检率后再批量上线
