# 批量任务调度指南

`scripts/batch_scheduler.py` 是算法脚本外的调度层，不修改 `video_mask_batch_flast.py`。它适合“一个源目录、很多视频、一个输出目录”的服务器任务。

## 特性

- 递归扫描源目录的视频；输出保留子目录层级，避免同名文件覆盖。
- 输出目录维护 `.video_mask_jobs.sqlite3`：记录源文件大小、修改时间、状态和失败原因。
- 已成功且可被 `ffprobe` 验证的输出会自动跳过；源视频内容变更后会重新入队。
- 中断、断开 SSH、服务器重启后直接重跑同一命令即可恢复。
- 单 GPU 队列顺序处理，避免多个 OWLv2 进程争抢显存；子任务日志实时显示，包含任务序号和 ETA。

## 基本运行

```bash
cd /home/ubuntu/video-mask

.venv/bin/python scripts/batch_scheduler.py \
  /data/video-mask/input \
  /data/video-mask/output \
  --extra-arg=--model-dir \
  --extra-arg=/home/ubuntu/video-mask/models \
  --extra-arg=--card-conf \
  --extra-arg=0.35
```

开始前可先查看执行计划：

```bash
.venv/bin/python scripts/batch_scheduler.py \
  /data/video-mask/input /data/video-mask/output --dry-run
```

## 恢复与重试

```bash
# 默认：只处理新任务、源文件变更任务与上次中断的任务
.venv/bin/python scripts/batch_scheduler.py /data/video-mask/input /data/video-mask/output

# 额外重试上次失败的任务
.venv/bin/python scripts/batch_scheduler.py \
  /data/video-mask/input /data/video-mask/output --retry-failed

# 忽略状态和已有结果，全部重跑（谨慎使用）
.venv/bin/python scripts/batch_scheduler.py \
  /data/video-mask/input /data/video-mask/output --force
```

不要把输出目录放在源目录内；若必须这样做，调度器会自动排除该输出子目录，避免把自己的结果再次作为输入。任何名为 `masked_*` 的历史结果也会自动跳过。

## 长任务运行

建议在 `tmux` 中执行：

```bash
tmux new -s video-mask
cd /home/ubuntu/video-mask
.venv/bin/python scripts/batch_scheduler.py /data/video-mask/input /data/video-mask/output
# Ctrl-b d 断开；任务继续运行
```

另一窗口观察 GPU：

```bash
nvidia-smi dmon -s pucm -d 1
```

当前调度器刻意不启用算法脚本的 `--pipe`；现有测试显示该模式可能生成损坏画面。
