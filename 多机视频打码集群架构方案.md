# 多机视频打码集群架构方案

## 结论

控制机同时承担两项职责：

1. **Ansible 控制节点**：批量初始化、升级、校验 Worker 机器；
2. **任务分发管理服务**：保存任务、分配视频、接收进度、处理失败重试。

两者部署在同一台控制机完全可行。它们是两个独立服务：Ansible 不参与每个视频的实时调度，也不经手大视频文件。

```text
管理员 / Web 页面
        │
        ▼
┌───────────────────────────────────────────────────────┐
│ 控制机                                                │
│                                                       │
│  任务分发服务（API / Web UI / SQLite WAL 任务库）       │
│  ├─ 创建任务、分配 Worker、进度与日志、失败重试        │
│  └─ 保存对象存储的临时下载/上传地址                   │
│                                                       │
│  Ansible 控制节点                                      │
│  └─ SSH 批量部署、升级、健康检查 Worker                │
└───────────────┬───────────────────────────────────────┘
                │ HTTPS 心跳、领取任务、上报状态
      ┌─────────┼─────────┐
      ▼         ▼         ▼
  Worker 01  Worker 02  Worker N
   T4/GPU     T4/GPU     T4/GPU
      │         │         │
      └──── 直接访问对象存储（源视频 / 输出视频） ──────┘
```

## 职责边界

| 组件 | 负责事项 | 不负责事项 |
|---|---|---|
| Ansible | 安装依赖、部署代码、启动服务、GPU 自检、版本升级 | 分配单个视频任务、传输大视频、追踪实时处理进度 |
| 任务分发服务 | 任务队列、Worker 注册、心跳、进度、失败重试、权限 | 直接运行打码算法 |
| Worker Agent | 领取任务、下载、调用打码脚本、上传结果、上报状态 | 选择其他 Worker、保存全局任务状态 |
| 对象存储（COS/S3/MinIO） | 保存源视频、输出视频和可选日志 | 调度任务 |
| `video_mask_batch_skip.py` | 单个视频的人脸打码执行 | 多机协作与任务状态管理 |

## 为什么不以 SSH/Ansible 分发每个视频

用 SSH 让控制机逐台启动命令可以作为临时方案，但规模超过 10 台后会出现以下问题：

- 控制机若先下载视频再 SCP 给 Worker，会成为带宽瓶颈；
- SSH 连接中断时，很难可靠判断视频任务是否仍在运行；
- 机器重启、IP 变更、任务超时后缺少自动领取与重试机制；
- Ansible 的异步任务只能负责“启动后台进程”，不适合承载数小时任务的状态机。

因此，SSH 仅用于 Ansible 的机器管理；Worker 使用出站 HTTPS 主动领取任务。这样 Worker 无需开放额外入站端口，也更容易适配不同云厂商的 IP 变化。

## Worker 的任务生命周期

```text
pending
  → assigned
  → downloading
  → processing
  → uploading
  → completed

任意阶段失败：failed → retry_pending → assigned
Worker 心跳超时：assigned / processing → retry_pending
```

每次任务都应保存：

- 输入视频 URL、原始文件大小和 SHA-256；
- 选择的算法版本及参数；
- Worker ID、GPU 型号、CUDA/ONNX Runtime/FFmpeg 信息；
- 开始时间、当前帧数、总帧数、实时速度、完成时间；
- 输出视频 URL、输出时长和 SHA-256；
- 失败原因及重试次数。

Worker 在本地使用临时文件，例如 `work/<job-id>/input.partial`、`output.partial.mp4`。只有校验通过并成功上传后，控制器才把任务标记为 `completed`。

## 视频传输方式

推荐源视频和输出视频都使用对象存储：腾讯 COS、AWS S3 或自建 MinIO。

```text
上传端 → 对象存储 source/<video-id>.MOV
Worker → 使用控制器下发的临时 URL 下载源视频
Worker → 处理完成后上传 outputs/<job-id>.mp4
控制器 → 保存对象 Key、元数据与下载链接
```

不要让控制器中转视频。控制机只传递 URL、任务元数据和状态；10 台 Worker 可并行下载而不会占用控制机出口带宽。

## 任务添加与自动入队

任务由控制器创建，Worker 不扫描共享目录，也不自行决定要处理哪些视频。日常默认入口是对象存储的固定目录：

```text
source/inbox/                 上传入口：上传成功后自动创建任务
source/accepted/              已成功入队的源视频（可选）
outputs/<task-id>/            Worker 上传的结果、日志和元数据
```

完整流程：

```text
管理员上传视频到 source/inbox/
→ 对象存储事件适配器向控制器提交任务
→ 控制器创建 pending 任务
→ 空闲 Worker 领取任务
→ Worker 下载、处理、上传
→ 控制器校验输出并标记 completed
```

控制器提供通用的 `POST /api/tasks` 入队接口。对象存储事件适配器、Web 页面和批量导入程序都调用该接口：

| 入口 | 使用方式 | 适用情况 |
|---|---|---|
| 自动入队（默认） | 上传文件到 `source/inbox/`，由对象存储事件触发 | 持续、大批量上传 |
| Web 页面 | 选择一个或多个文件，浏览器用预签名 URL 直传对象存储 | 人工少量上传 |
| 批量清单 | 上传 CSV/文本，内容为对象 Key 或已授权 URL | 已有大量历史视频 |

批量清单示例：

```text
source/2026-08/video_001.MOV
source/2026-08/video_002.MOV
source/2026-08/video_003.MOV
```

对象存储事件可能重复投递或偶发丢失，因此控制器必须同时具备两项保护：

1. **幂等入队**：以对象 Key + 文件大小 + SHA-256（或对象 ETag）作为去重依据；重复事件不能生成重复任务；
2. **定时补偿扫描**：周期性扫描 `source/inbox/`，将没有任务记录的已上传文件补入队列。

上传尚未完成的文件不能入队。上传端应先使用临时对象名，例如 `uploading/<uuid>.partial`，校验完成后再原子移动或复制到 `source/inbox/`，由事件触发任务创建。

### 任务记录

每个任务至少保存：

```text
task_id
source_object_key
source_size_bytes
source_sha256
source_duration_seconds
algorithm = video_mask_batch_skip.py
arguments = --no-card --face-size 960 --face-int 5 --frame-skip 3
status = pending | assigned | downloading | processing | uploading | completed | failed
attempt_count
assigned_worker_id
created_at / started_at / finished_at
output_object_key / output_sha256 / output_duration_seconds
error_message
```

任务提交后，控制器立即在页面显示队列位置、源视频时长和当前状态。Worker 每完成一个任务就再次请求任务；控制器仅向状态为 `ready` 的空闲 Worker 分配 `pending` 任务。

### Worker 领取任务接口

Worker Agent 的工作循环固定如下：

```text
POST /api/workers/register
POST /api/workers/<id>/heartbeat
POST /api/workers/<id>/claim
GET  预签名源视频下载 URL
POST /api/tasks/<task-id>/progress
PUT  预签名输出视频上传 URL
POST /api/tasks/<task-id>/complete 或 /fail
→ 再次 POST claim
```

Worker 必须先由 Ansible 通过受管理员 Token 保护的 `POST /api/admin/workers` 预注册；未注册机器或 Token 不匹配的机器不能领取任务。`claim` 请求携带 GPU 型号、可用显存、算法版本和当前并发数；控制器据此只向符合条件的空闲 Worker 分配任务。任务分配采用 SQLite WAL 的 `BEGIN IMMEDIATE` 事务锁定，确保同一任务不会被两台机器同时领取；单控制器进程管理十余台 Worker 的领取、心跳和进度写入是足够的。

## 当前实现与运行命令

项目包含以下集群组件：

```text
cluster/controller.py     控制器 FastAPI 服务与任务页面
cluster/store.py          SQLite WAL 任务、Worker、心跳与重试状态库
cluster/worker_agent.py   Worker 拉取、下载、执行、上传循环
ansible/                  Controller 与 GPU Worker 的 Ansible 角色、systemd 模板
```

控制器机器启动：

```bash
cd ~/video-mask
.venv/bin/python -m pip install -r requirements-cluster.txt
export VIDEO_MASK_ADMIN_TOKEN='替换为至少 16 位随机字符串'
.venv/bin/python -m cluster.controller \
  --database /var/lib/video-mask-controller/controller.sqlite3 \
  --host 0.0.0.0 --port 8080
```

上传程序或对象存储事件适配器使用预签名 URL 创建任务：

```bash
curl -X POST http://控制器地址:8080/api/tasks \
  -H "Authorization: Bearer $VIDEO_MASK_ADMIN_TOKEN" \
  -H 'Content-Type: application/json' \
  -d '{
    "source_url": "源视频临时下载 URL",
    "output_upload_url": "结果视频临时 PUT 上传 URL",
    "source_object_key": "source/inbox/video_001.MOV",
    "output_object_key": "outputs/video_001.mp4",
    "algorithm": "video_mask_batch_skip.py",
    "arguments": ["--no-card", "--face-size", "960", "--face-int", "5", "--frame-skip", "3"]
  }'
```

Worker 完成任务后会继续领取下一项。控制器任务页面位于 `http://控制器地址:8080/`；管理接口 `GET /api/tasks` 需要同一个 Bearer Token。

### 本地目录测试模式

在尚未获得第三方 S3 Bucket 权限时，控制器和 Worker 可以部署在**同一台测试机**，使用本地目录运行完整的领取、状态、重试流程：

```text
/home/ubuntu/sources/  控制器扫描的新视频
/home/ubuntu/outputs/  Worker 写入 masked_*.mp4
```

启动控制器时增加本地目录参数：

```bash
.venv/bin/python -m cluster.controller \
  --database /var/lib/video-mask-controller/controller.sqlite3 \
  --admin-token '至少 16 位随机字符串' \
  --local-source-dir /home/ubuntu/sources \
  --local-output-dir /home/ubuntu/outputs
```

Worker 使用 `--allow-local-files` 后，会接收受限的 `file://` 任务。每次领取任务前控制器扫描源目录；已有有效输出的源视频不会再次入队。该模式只用于单机测试，不能将一个 Worker 本地文件系统的路径分配给另一台机器。

Ansible 示例变量默认采用本地测试模式。`video_mask_storage_mode`、第三方源 Bucket、源前缀、跨账号 Role ARN、输出 Bucket/前缀/Region 都已预留；拿到 S3 权限与 Bucket 信息后，这些配置将用于启用 S3 入队适配器。

## Ansible 部署结构

在项目中使用独立的 Ansible 目录：

```text
ansible/
├── ansible.cfg
├── inventory.yml
├── site.yml
├── group_vars/
│   └── gpu_workers.yml
└── roles/
    └── video_mask_worker/
        ├── defaults/main.yml
        ├── tasks/main.yml
        ├── handlers/main.yml
        └── templates/video-mask-worker.service.j2
```

`inventory.yml` 示例：

```yaml
gpu_workers:
  vars:
    ansible_user: ubuntu
    ansible_python_interpreter: /usr/bin/python3
  hosts:
    worker-01:
      ansible_host: 43.130.141.49
      worker_gpu_id: 0
    worker-02:
      ansible_host: 43.130.141.50
      worker_gpu_id: 0
```

每个 Worker role 应当幂等地完成：

1. 安装 `ffmpeg`、`git`、Python 运行依赖；
2. 检查 `nvidia-smi`，失败则中止部署；
3. 部署指定版本的 `video-mask`；
4. 创建 Python 虚拟环境并安装项目依赖、`onnxruntime-gpu`；
5. 校验 PyTorch CUDA、ONNX Runtime CUDA Provider、NVENC；
6. 创建 `/home/ubuntu/work`、模型缓存目录和日志目录；
7. 写入每台机器唯一的 Worker ID、控制器地址和注册令牌；
8. 安装并启动 `video-mask-worker.service`，设为开机自启。

新增一台机器时的流程：

```text
购买并创建 GPU 主机
→ 配置 ubuntu 用户与 SSH 公钥
→ 在 inventory.yml 添加机器 IP 和 Worker ID
→ ansible-playbook -i inventory.yml site.yml --limit worker-xx
→ Worker Agent 自检并注册到控制器
→ 控制器显示 ready，开始领取任务
```

## 生产处理规则

每台 T4 Worker 的固定运行配置：

- 每张 T4 同时处理 **1 个视频任务**；
- 使用 `video_mask_batch_skip.py` 的默认参数：
  `--no-card --face-size 960 --face-int 5 --frame-skip 3`；
- 合成阶段使用 NVENC；
- Worker 每 15 秒上报一次进度和 GPU 指标。

一个视频只分配给一台 Worker；视频很多时，由控制器把**不同视频**分配给不同机器并行处理。该规则避免视频切片、边界漏检、拼接和音画同步风险，也使失败任务可以直接重新分配。

超长视频仍由单台 Worker 完整处理。Worker 必须使用独立工作目录、临时文件和断点状态；控制器仅在 Worker 心跳超时或任务失败后重新分配整个视频任务。

## 安全建议

- Ansible 使用专用 SSH 密钥，不使用密码登录；
- 每个 Worker 使用单独注册令牌，可单独吊销；
- Worker 仅需出站访问控制器与对象存储；
- 对象存储使用短时效预签名 URL，不向 Worker 下发长期密钥；
- 控制器数据库保存任务审计日志；
- 升级 Worker 时按批次滚动发布，例如一次 2 台，避免全量停机。

## 上线前必须完成的清单

1. 建立 Ansible inventory 和 `video_mask_worker` role；
2. 每台 T4 主机仅需添加 inventory 并执行一次 Ansible，即可完成环境、模型、GPU 检查和 Worker 服务启动；
3. 控制器包含 API、Web 页面和 SQLite WAL 任务库，Worker 注册后显示 GPU、算法参数和 `ready` 状态；
4. 上传视频后，空闲 Worker 自动领取并直接从对象存储下载；
5. 页面显示下载、处理、上传的进度、实时速度、GPU 状态与完整日志；
6. Worker 意外关机后，控制器在心跳超时后自动重新分配任务；
7. 输出视频完成校验并上传成功后，任务才标记为完成；
8. 以至少两台 Worker、多个视频连续运行完成一次全流程验收后，才接入全部机器。
