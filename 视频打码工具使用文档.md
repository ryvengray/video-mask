# 视频批量打码工具 — 使用文档

对视频中的人脸和信用卡/银行卡进行自动打码（马赛克模糊），支持批量处理、多种视频格式、保留原音轨。

---

## 1. 功能特性

| 功能 | 说明 |
|---|---|
| 👦 **人脸打码** | res10 SSD 深度学习模型检测（精度高），模型缺失时自动降级为 OpenCV 自带 Haar 级联（零依赖也能跑） |
| 💳 **信用卡/银行卡打码** | **OWLv2 深度学习模型**（默认，高准确率）：零样本识别"信用卡"，任意颜色、被手遮挡都能识别；另有几何检测法（快速备选） |
| 📦 **批量处理** | 支持单文件、多文件、整个文件夹、通配符 |
| 🎞 **多格式** | mp4 / mov / mkv / avi / webm / flv / m4v / mpg / mpeg / ts / m2ts / wmv / 3gp / rmvb / rm / vob / asf（大小写通吃） |
| 🎧 **保留音轨** | 自动调用系统 ffmpeg 抽帧/合成，音频无损复制 |
| 🛡 **防闪烁** | 时序传播 + 光流跟踪，漏检帧自动沿用上一帧位置 |

---

## 2. 环境要求

- **操作系统**：macOS / Linux / Windows（以下命令以 macOS 为例）
- **Python**：3.9+
- **磁盘空间**：约 2GB（含 OWLv2 模型约 600MB）
- **ffmpeg**（可选但推荐，保留音轨必需）

---

## 3. 安装依赖

### 3.1 安装 Python 基础依赖

```bash
# 推荐：在项目目录创建虚拟环境后安装项目本身
python3 -m venv .venv
source .venv/bin/activate
python -m pip install -e .
```

### 3.2 安装 OWLv2 卡识别依赖（高精度模式必需）

```bash
python -m pip install -e '.[cards]'
```

> torch 较大（约 200MB+），CPU 版即可。Mac 用户如遇网络问题可用国内镜像：
> ```bash
> pip install torch transformers pillow -i https://pypi.tuna.tsinghua.edu.cn/simple
> ```

### 3.3 安装 ffmpeg（保留音轨用）

```bash
# macOS (Homebrew)
brew install ffmpeg

# Ubuntu/Debian
sudo apt install ffmpeg

# Windows: https://ffmpeg.org/download.html 下载后加入 PATH
```

> 不装 ffmpeg 也能运行，但输出会**没有声音**。

### 3.4 模型下载

| 模型 | 大小 | 说明 |
|---|---|---|
| OWLv2（卡识别）| ~600MB | 首次运行时自动下载，缓存于 `~/.cache/huggingface/`。国内用户建议设置镜像：`export HF_ENDPOINT=https://hf-mirror.com`；已缓存模型时可加 `--offline` 禁止联网。 |
| res10 SSD（人脸）| ~6.5MB | 可选。不提供时自动用 Haar 级联（OpenCV 自带，无需下载） |

SSD 人脸模型文件（放同一目录，脚本通过 `--model-dir` 指定）：
- `deploy.prototxt`
- `res10_300x300_ssd_iter_140000.caffemodel`

---

## 4. 快速开始

```bash
# 处理单个视频（默认 OWLv2 高精度卡识别）
vmask 视频.mp4

# 输出: masked_out/masked_视频.mp4
```

### macOS 本机已配置环境时（推荐用法）

```bash
# 设别名（也可不设，直接用完整路径）
alias py='/Users/yaojie/.workbuddy/binaries/python/envs/default2/bin/python'

cd /Users/yaojie/workplace
py video_mask_batch.py IMG_7127.MOV
```

---

## 5. 命令行使用详解

```
vmask <输入...> [选项]
```

### 5.1 输入方式（必填）

| 示例 | 说明 |
|---|---|
| `python video_mask_batch.py a.mp4` | 单个视频 |
| `python video_mask_batch.py a.mp4 b.mov c.mkv` | 多个视频 |
| `python video_mask_batch.py ./videos/` | 整个文件夹（自动识别 17 种格式） |
| `python video_mask_batch.py "./videos/*.mov"` | 通配符 |

### 5.2 选项参数

| 参数 | 默认 | 说明 |
|---|---|---|
| `--out-dir 目录` | `masked_out` | 输出目录 |
| `--card-detector owlv2\|geo` | `owlv2` | 卡检测方式：`owlv2` 深度学习（高准确率）/ `geo` 几何法（快） |
| `--card-conf 数值` | `0.25` | OWLv2 置信度阈值，调低更易检出（误检略增），调高更严格 |
| `--card-color 颜色` | 无 | 几何法颜色提示：`yellow/gold/blue/green/red/orange/black/white`（卡被遮挡时用） |
| `--no-face` | 关 | 关闭人脸打码（只打码卡） |
| `--no-card` | 关 | 关闭信用卡打码（只打码人脸） |
| `--model-dir 目录` | 无 | SSD 人脸模型所在目录 |
| `--keep-tmp` | 关 | 保留中间帧（调试用） |
| `--offline` | 关 | OWLv2 仅使用本机缓存，不下载模型 |

### 5.3 完整示例

```bash
# 批量处理整个文件夹
python video_mask_batch.py ./videos/

# 卡被手拿着看不清 + 指定输出目录
python video_mask_batch.py 视频.mp4 --card-detector geo --card-color yellow --out-dir ./结果

# 只打码人脸
python video_mask_batch.py 视频.mp4 --no-card

# 调低卡识别阈值（更易检出，误检略增）
python video_mask_batch.py 视频.mp4 --card-conf 0.15
```

---

## 6. Python 代码调用

```python
from video_mask_batch import process_video

# 基本用法
process_video("输入.mp4", "输出.mp4")

# 完整参数
process_video(
    "输入.mp4", "输出.mp4",
    face_on=True,            # 人脸打码开关
    card_on=True,            # 信用卡打码开关
    card_detector="owlv2",   # 卡检测: "owlv2"(高精度) / "geo"(快速)
    card_conf=0.25,          # OWLv2 置信度阈值
    card_color=None,         # 几何法颜色提示
)

# 批量循环
import os
for f in os.listdir("videos"):
    if f.endswith(".mp4"):
        process_video(f"videos/{f}", f"out/{f}")
```

---

## 7. 卡检测方式对比

| 方式 | 准确率 | 速度 | 适用场景 |
|---|---|---|---|
| **OWLv2**（默认）| ⭐ 高 | 慢（约 6 分钟/10 秒 4K 视频）| 卡被手遮挡、任意颜色、复杂背景 |
| **几何法 geo** | 中 | 快（约 2 分钟/10 秒 4K 视频）| 卡边缘完整可见、对速度有要求 |

**说明**：
- OWLv2 是深度学习模型，认的是"信用卡"这个**概念**（不依赖颜色/边缘巧合），识别最可靠。
- 几何法用"卡的标准长宽比 1.586 + 卡面均匀 + 文字行"特征，边缘完整的卡效果好；卡被遮挡时可加 `--card-color 颜色` 提升。
- 速度慢的原因：OWLv2 每 25 帧检测一次（4K 帧约 9 秒），中间帧用光流跟踪跟随，在准确率和速度间取得平衡。

---

## 8. 输出说明

- 输出目录：默认 `masked_out/`（可用 `--out-dir` 修改）
- 命名规则：`masked_<原文件名>.mp4`
- 格式：H.264 + 原音轨（AAC 无损复制）
- 分辨率/帧率与源视频一致

---

## 9. 性能参考

| 视频 | 时长 | 分辨率 | 耗时（OWLv2）|
|---|---|---|---|
| 测试视频 | 12 秒 | 4K 60fps（713 帧）| 约 6 分钟 |
| 常规 1080p 视频 | 1 分钟 | 1080p 30fps | 约 5-10 分钟 |

> 处理速度主要取决于帧数和卡检测次数。长视频建议分批次处理，或使用 `--card-detector geo` 加速。

---

## 10. 常见问题（FAQ）

**Q1: 报错 `ModuleNotFoundError: No module named 'cv2'`**
未安装 opencv。执行 `pip install opencv-python numpy`；或使用已配置好的 Python 环境。

**Q2: 系统 python3 装不了包（externally-managed-environment）**
macOS 系统 Python 受限，建议：
```bash
python3 -m venv myenv
source myenv/bin/activate
pip install opencv-python numpy torch transformers pillow
```

**Q3: OWLv2 模型下载慢/失败**
国内网络建议设置镜像后重跑：
```bash
export HF_ENDPOINT=https://hf-mirror.com
```
模型只需下载一次，之后离线可用（脚本已默认离线模式）。

**Q4: 输出没有声音**
未安装 ffmpeg。安装后重新运行（见 3.3 节）。

**Q5: 卡没有被识别出来（漏检）**
- 调低置信度：`--card-conf 0.15`
- 换几何法 + 颜色提示：`--card-detector geo --card-color yellow`
- 确保视频清晰、卡不要太远太小

**Q6: 误打码（把书本/背景当成卡）**
- OWLv2 模式调高阈值：`--card-conf 0.4`
- 几何法模式提高阈值：手动调整脚本中 `GEO_SCORE`

**Q7: 处理很慢**
- 用 `--card-detector geo` 快速模式
- 或者先裁剪视频长度/降分辨率再处理

---

## 11. 文件清单

| 文件 | 说明 |
|---|---|
| `video_mask_batch.py` | 主程序（自包含，人脸+卡打码、批量、多格式）|
| `card_detector.py` | 银行卡几何检测模块（可选，OWLv2 模式下不需要）|
| `masked_out/` | 输出目录（自动创建）|

> 主程序为单文件自包含设计，复制 `video_mask_batch.py` 到任何机器，装好依赖即可运行。
